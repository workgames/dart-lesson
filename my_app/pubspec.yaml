name: my_app
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>= 3.1.0 < 4.0.0'

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  mad_package:
    # Настройки пакета
    path: ../mad_package

#мОЖНО ВЫЛОЖИТЬ ПАКЕТ В ГИТ
  #mad_package:
  # git: https:-/github.com/MADTeacher/mad_package.gi    

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
